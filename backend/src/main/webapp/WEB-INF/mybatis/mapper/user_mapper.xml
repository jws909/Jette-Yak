<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.mapper.UserMapper">

    <select id="countByLoginId" resultType="int">
        SELECT COUNT(*) FROM users WHERE login_id = #{loginId}
    </select>

    <select id="countByEmail" resultType="int">
        SELECT COUNT(*) FROM users WHERE email = #{email}
    </select>

    <select id="countByNickname" resultType="int">
        SELECT COUNT(*) FROM users WHERE nickname = #{nickname}
    </select>

    <select id="countByNicknameExceptLoginId" resultType="int">
        SELECT COUNT(*) FROM users
        WHERE nickname = #{nickname} AND login_id != #{loginId}
    </select>

    <select id="findByLoginId" resultType="com.app.domain.User">
        SELECT user_id AS userId, login_id AS loginId, password_hash AS passwordHash,
               email, nickname, profile_image_url AS profileImageUrl,
               NVL(breakfast_time, '07:30') AS breakfastTime,
               NVL(lunch_time, '12:00') AS lunchTime,
               NVL(dinner_time, '18:30') AS dinnerTime,
               NVL(bedtime, '22:00') AS bedtime
        FROM users
        WHERE login_id = #{loginId}
    </select>

    <select id="findById" resultType="com.app.domain.User">
        SELECT user_id AS userId, login_id AS loginId, password_hash AS passwordHash,
               email, nickname, profile_image_url AS profileImageUrl,
               NVL(breakfast_time, '07:30') AS breakfastTime,
               NVL(lunch_time, '12:00') AS lunchTime,
               NVL(dinner_time, '18:30') AS dinnerTime,
               NVL(bedtime, '22:00') AS bedtime
        FROM users
        WHERE user_id = #{userId}
    </select>

    <select id="findByEmail" resultType="com.app.domain.User">
        SELECT user_id AS userId, login_id AS loginId, email, nickname
        FROM users
        WHERE LOWER(email) = LOWER(#{email})
    </select>

    <update id="updatePasswordHash">
        UPDATE users
        SET password_hash = #{passwordHash}, updated_at = CURRENT_TIMESTAMP
        WHERE login_id = #{loginId}
    </update>

    <update id="updateNickname">
        UPDATE users SET nickname = #{nickname}, updated_at = CURRENT_TIMESTAMP
        WHERE login_id = #{loginId}
    </update>

    <update id="updateProfileImageUrl">
        UPDATE users SET profile_image_url = #{profileImageUrl}, updated_at = CURRENT_TIMESTAMP
        WHERE login_id = #{loginId}
    </update>

    <update id="updateMealTimes">
        UPDATE users
        SET breakfast_time = #{breakfastTime},
            lunch_time = #{lunchTime},
            dinner_time = #{dinnerTime},
            bedtime = #{bedtime},
            updated_at = CURRENT_TIMESTAMP
        WHERE <choose>
                <when test="userId != null">user_id = #{userId}</when>
                <otherwise>login_id = #{loginId}</otherwise>
              </choose>
    </update>

    <!--
      user_id: IDENTITY 컬럼이라 생략(자동 채번)
      created_at / updated_at / push_enabled / role: DB 기본값 사용을 위해 생략
      family_id / profile_image_url / med_updated_at: 회원가입 단계에서는 값 없음 (nullable)
      is_pregnant: 기본값이 없는 컬럼이라 항상 값 전달 필요
    -->
    <insert id="insertUser" parameterType="com.app.domain.User">
        INSERT INTO users (
            login_id, password_hash, email, nickname, sex, birthdate, is_pregnant
            <if test="tel != null">, tel</if>
        ) VALUES (
            #{loginId}, #{passwordHash}, #{email}, #{nickname}, #{sex}, #{birthdate}, #{isPregnant}
            <if test="tel != null">, #{tel}</if>
        )
    </insert>

</mapper>
